stages:
  - check
  - test
  - build

variables:
  CARGO_TERM_COLOR: always
  SQLX_OFFLINE: "true"

cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - target/

# 1. CHECK
check:
  image: rust:1.96
  stage: check
  before_script:
    - export PATH="/usr/local/rustup/toolchains/1.96.0-x86_64-unknown-linux-gnu/bin:$PATH"
    - rustup component add clippy
  script:
    - cargo clippy -- -D warnings
    - cargo check

# 2. TEST
test:
  image: rust:1.96
  stage: test
  services:
    - name: postgres:15-alpine
      alias: postgres
  variables:
    POSTGRES_DB: doodoo_test
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: postgres
    DATABASE_URL: "postgres://postgres:postgres@postgres:5432/doodoo_test"
  before_script:
    - export PATH="/usr/local/rustup/toolchains/1.96.0-x86_64-unknown-linux-gnu/bin:$PATH"
  script:
    # 1. Run unit tests embedded inside your main application binary
    - cargo test --bin doodoo-logistics-rust
    # 2. Run integration tests explicitly by pointing to their targets
    - cargo test --test payment_it --test shipment_it

# 3. BUILD
build:
  image: rust:1.96
  stage: build
  script:
    - cargo build --release