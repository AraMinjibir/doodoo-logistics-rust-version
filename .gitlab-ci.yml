stages:
  - build
  - test

build:
  image: rust:1.95
  stage: build
  script:
    - cargo build --verbose

test:
  image: rust:1.95
  stage: test
  script:
    - cargo test --verbose

variables:
  SQLX_OFFLINE: "true"    